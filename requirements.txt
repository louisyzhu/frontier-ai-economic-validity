# Analysis environment (Python 3.13).
# The notebook applies a small compatibility shim for factor_analyzer on
# scikit-learn >= 1.7 (check_array's force_all_finite was renamed
# ensure_all_finite); the shim is at the top of the EDA/Task-1 section.
requests==2.34.2
numpy==2.4.6
pandas==3.0.3
scipy==1.18.0
scikit-learn==1.9.0
factor_analyzer==0.5.1
statsmodels==0.14.6
matplotlib==3.11.0
seaborn==0.13.2
shap==0.52.0
jupyter
